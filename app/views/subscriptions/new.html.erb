<%= form_for @subscription, remote: true, :validate => true, :html => {:'data-type' => 'json', :multipart => true} do |f| %>
    <div class="email-input">
        <%= f.text_field :email, placeholder: 'email' %>
        <%= f.submit 'Присоединиться', id: 'subscribe-button' %>
    </div>
<% end %>